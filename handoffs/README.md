# handoffs/

Framework-agnostic automation handoffs produced by `/gqa handoff`:
`handoffs/<project>-<task>-automation.md`, format
`templates/automation-handoff.md`.

These are optional downstream outputs for an automation repository. They never
contain framework code. For private handoffs use `handoffs/_local/…`
(git-ignored).
